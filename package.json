{
  "name": "ether-wallet",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://acpixel.github.io/react-eth-wallet/",
  "dependencies": {
    "ethers": "^3.0.3",
    "react": "^16.2.0",
    "react-dom": "^16.2.1",
    "react-scripts": "1.1.1",
    "snyk": "^1.115.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "snyk-protect": "snyk protect",
    "prepare": "npm run snyk-protect"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  },
  "snyk": true
}
